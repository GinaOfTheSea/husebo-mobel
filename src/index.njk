---
layout: default
title: Neat Starter
path: home
---

<section class="py-20 bg-gray-50">
  <div class="container mx-auto px-6">
    <h1 class="text-4xl font-bold text-gray-900">Vi realiserer dine ønsker</h1>
    <p class="mt-4 text-lg text-gray-600 max-w-2xl">
      Vi har bred kompetanse, og tar på oss både små og store utfordringer.
    </p>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mt-12">
      <div class="p-6 bg-white shadow-sm rounded-lg border-t-4 border-amber-600">
        <h3 class="font-bold text-xl">Innredning</h3>
        <p class="text-sm text-gray-500 mt-2">Ønsker du utnytte plassen optimalt? Vi kan lage hyller og skap...</p>
      </div>
      </div>
  </div>
</section>

<!-- Image section -->
<div x-data="{ open: false, activeImage: '', activeTitle: '' }">
  
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 p-6">
    {% for item in collections.portfolio %}
    <div class="cursor-pointer group" @click="open = true; activeImage = '{{ item.data.photo }}'; activeTitle = '{{ item.data.title }}'">
      <div class="overflow-hidden rounded-lg shadow-lg bg-gray-200">
        <img src="{{ item.data.photo }}" alt="{{ item.data.title }}" class="w-full h-64 object-cover transform group-hover:scale-105 transition duration-300">
      </div>
      <h3 class="mt-2 text-sm font-medium text-gray-700">{{ item.data.title }}</h3>
    </div>
    {% endfor %}
  </div>

  <div x-show="open" 
       x-transition.opacity
       class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-90 p-4"
       @keydown.escape.window="open = false"
       style="display: none;">
    
    <button @click="open = false" class="absolute top-5 right-5 text-white text-4xl">&times;</button>

    <div @click.away="open = false" class="max-w-5xl w-full flex flex-col items-center">
      <img :src="activeImage" class="max-h-[85vh] object-contain shadow-2xl">
      <p x-text="activeTitle" class="text-white mt-4 text-xl font-light italic"></p>
    </div>
  </div>

</div>

<section class="bg-gray-900 text-white py-16">
  <div class="container mx-auto px-6 flex flex-col md:flex-row items-center">
    <div class="md:w-1/3">
       <div class="w-48 h-48 bg-gray-700 rounded-full mb-6 mx-auto"></div>
    </div>
    <div class="md:w-2/3">
      <h2 class="text-3xl font-bold">Tommy Husebø</h2>
      <p class="text-amber-500 font-medium">Møbelsnekker, 3D-designer, CNC-operatør</p>
      <p class="mt-4 text-gray-400">
        Tommy har over 25 års erfaring... bruker 3D design og datastyrte maskiner for beste kvalitet.
      </p>
      <div class="mt-6">
        <a href="mailto:post@husebo-mobel.no" class="underline text-amber-500">post@husebo-mobel.no</a>
        <p class="mt-1">970 97 546</p>
      </div>
    </div>
  </div>
</section>
